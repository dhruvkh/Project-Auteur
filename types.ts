
export interface NavLink {
  name: string;
  path: string;
}

export interface Book {
  id: string;
  title: string;
  author: string;
  imageUrl: string;
}

export interface Service {
  title: string;
  description: string;
}
